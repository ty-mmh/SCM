# v0.1実装仕様に残る空白

- 状態: non-normative open questions
- 対象: Generative Model Succession Profile v0.1
- 最終更新: 2026-08-04

## V01-01 — 日次Narrative運用境界

- 状態: `provisional`
- 仮決め: Narrative Processは日次でRevisionを生成・コミットする。

### 空白

- 日付境界のタイムゾーン。
- Eventがゼロの日にRevisionを作るか。
- Systemが`dormant`でも処理するか。
- 失敗時の再試行回数と時刻。
- 遅延実行したRevisionをどの日へ帰属させるか。
- 同日に複数Revisionを許すか。
- 前日分が未生成のまま翌日へ進んだ場合の扱い。

### 決定が必要になる時点

Narrative schedulerを実装する前。

---

## V01-02 — Narrative入力集合の閉包規則

- 状態: `open`

### 既に決まっていること

Narrative Processは、直前Revision、Event、MemoryEntry、Task経過、個体交代来歴を材料にできる。

### 空白

- 当日Eventを全件読むか。
- `pending` MemoryEntryを含めるか。
- `invalidated` MemoryEntryをどう扱うか。
- 矛盾関係を必ず同伴させるか。
- 未完了Taskの途中経過を含めるか。
- 外部参照資料をどこまで入力に含めるか。
- 入力上限を超えた場合の剪定規則。
- 実際に使った入力参照集合を完全保存するか。

### 決定が必要になる時点

Narrative Process APIと`NarrativeRevision`スキーマを固定する前。

---

## V01-03 — Narrative生成・コミットの原子性

- 状態: `open`

### 空白

- 生成とコミットを同一トランザクションにするか。
- 中間的なNarrative candidateを持つか。
- 同じ日次処理の二重実行をどう冪等化するか。
- 生成中にEventが追加された場合の入力境界。
- `parent_narrative_id`の競合処理。
- content保存後に`narrative_head_id`更新が失敗した場合。
- Narrative計算モデルが中断・失敗した場合。
- 形式不正・空出力をどう扱うか。

### 決定が必要になる時点

Narrativeの永続化処理を実装する前。

---

## V01-04 — MemoryEntry一件の粒度

- 状態: `open`

### 既に決まっていること

個体の行為または非行為から`pending` MemoryEntryを自動生成する。

### 空白

一件のMemoryEntryを、次のどの単位で切るか。

- 一回のモデル応答全体。
- 応答内の一主張。
- 一回のツール要求。
- ツール要求と結果の一組。
- Taskの一Step。
- 一回の拒否・保留。
- 一定期間継続した沈黙・非行為。

粒度を統一するか、`action_type`ごとに異なる境界を認めるかも未決。

### 決定が必要になる時点

Event→MemoryEntry自動生成処理を実装する前。

---

## V01-05 — MemoryEntryの結果接続と閉包

- 状態: `open`

### 既に決まっていること

結果Eventが接続された時点で`pending → closed`へ移る。

### 空白

- 一つの行為に複数結果がある場合。
- 複数行為が一つの結果を生んだ場合。
- 結果が数日後に発生する場合。
- 結果が観測されないまま残る場合。
- 「結果未観測」を結果として閉じる条件。
- closed後に追加結果が現れた場合。
- 誤った結果Eventへ接続した場合。
- closedを再度開くか、新しいMemoryEntryを形成するか。

### 決定が必要になる時点

Memory closure serviceとケースC3を実装する前。

---

## V01-06 — MemoryEntryの正規失効経路

- 状態: `open`

### 空白

- `invalidated`を付与できる正規主体。
- Verifier結果による自動失効の有無。
- 現在個体の判断だけで失効できるか。
- ユーザーの外部主張を失効根拠にできるか。
- 新しい反証Eventと失効の関係。
- 失効後もRecallへ返す条件。
- 失効の撤回を認めるか。

### 決定が必要になる時点

`memory_invalidated` EventとRecall境界を実装する前。

---

## V01-07 — 矛盾関係の登録

- 状態: `open`

### 既に決まっていること

矛盾するMemoryEntryを一つへ溶かさず、`contradiction_refs`で保持する。

### 空白

- 矛盾を検出・登録する主体。
- 明示的な反対内容と、単なる異なる解釈の区別。
- v0.1で自動検出するか、ケース入力として明示するか。
- ユーザー主張を矛盾候補へ含めるか。
- 後から関係が解消された場合も参照を残すか。

### 決定が必要になる時点

C3とRecallItemの実装前。

---

## V01-08 — Recallの最小選択規則

- 状態: `open`

### 既に決まっていること

v0.1ではNarrative、`recall_weight`、自動忘却をRecall選択へ使用しない。

### 空白

- Task ID一致を使うか。
- Handoffへ列挙されたMemoryEntryだけを使うか。
- 明示ID検索を中心にするか。
- 文字列検索・埋め込み検索を使うか。
- `pending`を返すか。
- `invalidated`を返すか。
- 矛盾相手を必ず同伴させるか。
- 件数上限と時間範囲。
- 自己記憶と他個体記憶の順位差。
- 根拠Eventをどの粒度で返すか。

### 決定が必要になる時点

Recall APIとC2を実装する前。

---

## V01-09 — Handoffへ含める資料の選択規則

- 状態: `open`

### 空白

- Task関連Eventを全件含めるか。
- `event_cursor`以後の差分だけを含めるか。
- `pending` MemoryEntryを全件含めるか。
- closed MemoryEntryをどう選ぶか。
- `invalidated` MemoryEntryを含めるか。
- contradiction関係をどこまで展開するか。
- Taskがない期間の来歴を含めるか。
- Narrative本文ではなくRevision IDだけで十分か。
- Handoffサイズ上限と縮約規則。

### 決定が必要になる時点

Handoff builderとC1・C3・C5を実装する前。

---

## V01-10 — Handoff鮮度と版境界

- 状態: `open`

### 空白

次のどれが変化したら`refresh_required`とするか。

- Task version。
- Event cursor。
- 共有状態version。
- Narrative Revision ID。
- Task権限・制約version。
- 能力要件。

独立したSharedStateエンティティを持たないv0.1で、共有状態の版をどう表すかも未決。

### 決定が必要になる時点

Handoff validationとC5を実装する前。

---

## V01-11 — Handoff受領・受諾・束縛の順序

- 状態: `open`

### 既に決まっていること

Handoffは宛先未確定で生成され、Handoff本体を変更せずEventで後継個体へ束縛する。

### 空白

- `received / evaluated / accepted / bound / activated`の厳密な順序。
- 受領時点で束縛するか、受諾後に束縛するか。
- 束縛と受諾を同一Eventにするか。
- `refresh_required`時に一時束縛を認めるか。
- 複数Instanceが同じHandoffを受領した場合。
- 拒否後に別Instanceへ渡せるか。
- 束縛後に後継Instanceが停止した場合。
- 後継が見つからない場合のSystem状態。

### 決定が必要になる時点

Handoff state machineを実装する前。

---

## V01-12 — Task状態機械

- 状態: `open`

### 既に決まっていること

Taskは、契機、局所目的、状態、完了条件、権限・制約、成果物を持つ独立構造である。

### 空白

- `status`の集合と遷移。
- Taskを作成できる主体。
- 局所目的・完了条件を変更できる主体。
- 権限・制約の型と版管理。
- 完了申告とVerifier結果の関係。
- 中止、放棄、失効、失敗の区別。
- ユーザーによる中断操作。
- 長期Taskを日次Narrativeへどう入力するか。

### 決定が必要になる時点

Task repository・projection・C1・C3・C4を実装する前。

---

## V01-13 — 共有状態Projection

- 状態: `open`

### 既に決まっていること

共有状態は真実そのものではなく、観測・申告・推定・作業状態をまとめた運用上の共有信念状態である。

### 空白

- Eventから都度Projectionするか。
- Task状態へ含めるか。
- 一時キャッシュとして管理するか。
- 共有状態のversion表現。
- 鮮度と有効期限。
- 不一致状態の構造。
- Handoffが参照する共有状態版。

### 決定が必要になる時点

C3・C5とHandoff鮮度判定を実装する前。

---

## V01-14 — System／Instance状態遷移

- 状態: `open`

### Systemの空白

- `active / dormant / ended`の遷移。
- dormantへ移す条件。
- dormantからactiveへ戻す条件。
- endedを不可逆とするか。
- dormant中の日次Narrative処理。

### Instanceの空白

- `registered / active / inactive / stopped`の厳密な意味。
- inactiveとstoppedの差。
- 同じ`instance_id`の再activation。
- プロセスクラッシュ時の状態。
- 同一モデル再起動時に必ず新`instance_id`を作るか。

### 決定が必要になる時点

System・Instance repositoryとactivation gateを実装する前。

---

## V01-15 — Taskなしモデル切替

- 状態: `open`
- 予定: 新しいCoreケースではなく、C1のvariantとして扱う候補。

### 空白

- active Taskがない場合にHandoffを生成するか。
- 何を後継個体へ渡すか。
- 正典Narrative、原則、未解決MemoryEntryだけで十分か。
- Taskなし期間の個体交代をNarrativeへどう残すか。

### 決定が必要になる時点

C1 variantとSystem-level Handoffを実装する前。
