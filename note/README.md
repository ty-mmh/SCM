# SCM Note Directory

- 状態: non-normative workbench
- 役割: 未決事項、仮置き、延期事項、未実証事項を、正典と混ぜずに管理する
- 正典: `docs/01`、`docs/02`、`docs/03`、`docs/07`、`docs/08`

## 1. 権威境界

`note/`配下の文書は、SCMの正典・参照プロファイル・適合条件を変更しない。

Noteに書かれている選択肢、仮説、実装案は、次を満たすまでSCMの決定ではない。

1. 未決事項に対して明示的な決定が置かれる。
2. 決定の出所が記録される。
3. 影響する正典文書・参照プロファイル・ケースが同期される。
4. 既存構造との衝突と剪定対象が確認される。

> **空白は、決定されるまで空白のまま保持する。実装上自然に見える補完を、正典判断へ自動昇格させない。**

## 2. 状態

| 状態 | 意味 |
|---|---|
| `open` | 決定が必要で、まだ回答がない |
| `provisional` | 仮決めがあるが、実装・検算で変更しうる |
| `deferred` | SCMの射程にあるが、現在のプロファイルでは扱わない |
| `out_of_scope` | 現行SCMの責務として扱わない |
| `unvalidated` | 概念または仕様は置かれたが、実装・ケース・外部レビューで未確認 |
| `resolved` | 明示的決定が正典へ同期済み |

`resolved`になった項目は、正典へのリンクと同期コミットを残したうえでNoteから削除せず、履歴として保持できる。

## 3. 項目形式

```text
ID:
状態:
対象:
既に決まっていること:
空白:
決定が必要になる時点:
正典へ反映する場合の影響先:
出所・経緯:
```

## 4. 現在のNote

1. [概念核に残る空白](01-core-open-questions.md)
2. [v0.1実装仕様に残る空白](02-v0.1-open-questions.md)
3. [延期・範囲外・未実証](03-deferred-and-unvalidated.md)

## 5. 更新規則

- 新しい空白を発見した場合、正典へ直接補完せず、まずNoteへ追加する。
- 会話内で回答が置かれた場合、その回答とアシスタント提案を区別する。
- 一項目の決定が別の空白を生んだ場合、別IDとして追加する。
- 実装詳細で閉じられる事項と、存在論的な判断が必要な事項を分ける。
- `deferred`は不要を意味しない。
- `unvalidated`は誤りを意味しない。
- Noteを理由にv0.1 Coreケースを増やさない。必要なら既存ケースのvariant、置換、統合を検討する。
